import styled from "styled-components";


export  const MetodoTitle = styled.h2`
    text-transform:uppercase;
    font-size:1rem;
    text-align:center;
    font-weight:bold;
`;

export  const MetodoOL = styled.ol`
    list-style: decimal;
`;

export  const MetodoItem = styled.li`
    font-size:1rem;
    color:#39393A;
    padding:.5rem;

`;